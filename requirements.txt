# Core
pymupdf
pywin32
click
rich

# Backends (all installed by default on supported Python versions)
pdfplumber
# marker-pdf pins Pillow<11 and regex<2025 — no Py3.14+ Windows wheels; use Python 3.12–3.13 for Marker
marker-pdf; python_version < "3.14"
docling

# Vertex AI backend (optional — install to enable Gemini extraction)
google-genai
python-dotenv

# Streamlit UI
streamlit

# Dev/test
pytest
pytest-cov
