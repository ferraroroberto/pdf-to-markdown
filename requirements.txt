# Core
pymupdf
pywin32; sys_platform == "win32"
click
rich

# Office-document pre-conversion (Linux/Unix path; Windows uses the Office COM API)
docling

# Hub Gemini backend (default) — routes PDFs through the local LLM hub
# (http://127.0.0.1:8000) via the Anthropic SDK shape with document blocks.
anthropic

# Vertex AI backend (fallback — install to enable direct Gemini extraction)
google-genai
python-dotenv

# Streamlit UI
streamlit

# Dev/test
pytest
pytest-cov
