# Core
pymupdf>=1.24.0
pywin32>=306  # Office COM automation (Windows only, requires Microsoft Office)
click>=8.0
rich>=13.0

# Backends (all installed by default)
pdfplumber>=0.11.0
marker-pdf>=1.0.0
docling>=2.50.0

# Vertex AI backend (optional — install to enable Gemini extraction)
google-genai>=1.0.0
python-dotenv>=1.0.0

# Streamlit UI
streamlit>=1.35.0

# Dev/test
pytest>=8.0
pytest-cov
